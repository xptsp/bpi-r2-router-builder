# defaults for transmission-daemon
# sourced by /etc/init.d/transmission-daemon
# sourced by /lib/systemd/system/transmission-daemon.service.d/changes.conf

# Username for transmission-daemon WebUI (default: pi)
TRANS_USER=pi

# Password for transmission-daemon WebUI (default: bananapi)
TRANS_PASS=bananapi

# Folder for torrents to be downloaded into (default: /home/vpn/Completed)
TRANS_DOWNLOAD=/home/vpn/Completed

# Folder where incomplete torrents are stored until completion (default: /home/vpn/Incomplete)
TRANS_INCOMPLETE=/home/vpn/Incomplete

# Subdirectory to use for transmission-daemon WebUI (default: transmission-web-control/src)
TRANS_WEBUI=transmission-web-control/src

# Change to 0 to disable daemon
ENABLE_DAEMON=1

# This directory stores some runtime information, like torrent files 
# and links to the config file, which itself can be found in 
# /etc/transmission-daemon/settings.json
CONFIG_DIR="/var/lib/transmission-daemon/info"

# Default options for daemon, see transmission-daemon(1) for more options
OPTIONS=""

# (optional) extra options to start-stop-daemon
#START_STOP_OPTIONS="--iosched idle --nicelevel 10"

